# PixiJS 版本功能檢查報告

## 1. 屍體顯示 (Corpse Display)

### 現狀
- ❌ **PixiJS 版本目前沒有實現屍體顯示**
- ✅ DOM 版本有屍體顯示功能

### 需要實現的功能
1. 在棋盤格子上顯示屍體圖示
2. 支持多個屍體堆疊顯示
3. 屍體高亮（骸骨煉化道具選擇目標時）
4. 屍體數量顯示

### 實現位置
- `PixiBoardRenderer.ts` - 需要添加屍體渲染邏輯
- `PixiBoard.vue` - 需要傳遞 `corpsesByPos` 數據
- 參考 `BoardGrid.vue` 的 `highlightCorpsePosKeys` 實現

---

## 2. 道具使用特效 (Item Usage Effects)

### 現狀
- ⚠️ **部分實現，需要逐一確認**

### 需要檢查的道具
1. **靈血聖杯** (item_lingxue_holy_grail) - 治療效果 ✅ (已有 heal text)
2. **亡者歸途** (item_dead_return_path) - 移除附魔效果 ❓
3. **骸骨煉化** (item_bone_refine) - 移除屍體效果 ❓
4. **冥鎖封印** (item_nether_seal) - 封印效果 ❓
5. **牢籠掠奪** (item_cage_plunder) - 奪取靈魂卡效果 ❓
6. **殺戮狂歡** (item_kill_feast) - 回復魔力效果 ❓

### 實現建議
在 `EffectsManager.ts` 中添加：
- `createItemUseEffect(itemId, x, y)` - 通用道具使用特效
- 針對不同道具顯示不同視覺效果

---

## 3. 能力觸發特效 (Ability Trigger Effects)

### 現狀
- ❌ **PixiJS 版本沒有實現能力觸發特效**

### 需要實現的能力特效
1. **獻祭** (SACRIFICE) - 摧毀友軍的視覺效果
2. **連鎖射擊** (CHAIN) - 連鎖目標的視覺連接
3. **穿透** (PIERCE) - 穿透路徑的視覺效果 ⚠️ (部分實現)
4. **濺射** (SPLASH) - 濺射範圍的視覺效果
5. **光環** (AURA) - 光環效果的視覺提示
6. **死亡觸發** (ON_DEATH) - 死亡時的特殊效果
7. **擊殺觸發** (ON_KILL) - 擊殺時的特殊效果

### 實現建議
- 在 `EffectsManager.ts` 中為每種能力類型創建對應的視覺效果
- 參考 DOM 版本的 CSS 動畫效果

---

## 4. 狀態圖示 (Status Icons)

### 現狀
- ❌ **PixiJS 版本沒有實現狀態圖示**

### 需要實現的狀態
1. **封印** (DISABLE_UNIT_ACTIONS) - 顯示封印圖示
2. **Buff/Debuff** - 各種增益/減益效果圖示
3. **臨時效果** - 回合結束消失的效果標記

### 實現位置
- `UnitSprite.ts` - 在單位精靈上添加狀態圖示容器
- 需要設計圖示資源或使用文字/符號表示

---

## 5. 其他缺失功能

### 5.1 屍體相關
- ❌ 屍體渲染
- ❌ 屍體高亮（骸骨煉化選擇）
- ❌ 屍體數量顯示

### 5.2 視覺反饋
- ❌ 道具使用特效
- ❌ 能力觸發特效
- ❌ 狀態圖示

### 5.3 已實現功能
- ✅ 基礎棋盤渲染
- ✅ 棋子顯示與移動
- ✅ 攻擊/射擊特效
- ✅ 傷害/治療數字
- ✅ 附魔特效
- ✅ 復活特效
- ✅ 選擇高亮
- ✅ 移動/攻擊目標高亮

---

## 優先級建議

### 高優先級 (必須實現才能替代 DOM)
1. **屍體顯示** - 影響遊戲核心玩法（復活、骸骨煉化）
2. **封印狀態圖示** - 影響遊戲邏輯理解

### 中優先級 (增強體驗)
3. **道具使用特效** - 提升視覺反饋
4. **能力觸發特效** - 幫助理解遊戲機制

### 低優先級 (錦上添花)
5. **其他 Buff/Debuff 圖示** - 可以先用文字說明

---

## 實現計劃

### 第一階段：屍體系統
1. 在 `PixiBoardRenderer.ts` 添加屍體容器
2. 創建屍體精靈類 `CorpseSprite.ts`
3. 實現屍體渲染和更新邏輯
4. 添加屍體高亮功能

### 第二階段：狀態圖示
1. 在 `UnitSprite.ts` 添加狀態圖示容器
2. 實現封印圖示顯示
3. 添加其他常用狀態圖示

### 第三階段：特效增強
1. 為各種道具添加使用特效
2. 為能力觸發添加視覺效果
3. 優化現有特效的表現

---

## 附魔特效問題排查

### 可能原因
1. 編譯錯誤導致代碼未更新
2. 動畫時間太短看不到
3. 圖層被遮擋
4. 座標計算錯誤

### 調試步驟
1. 刷新頁面
2. 執行附魔動作
3. 查看控制台日誌：
   - `🎆 Enchant effect triggered`
   - `✨ Creating enchant effect at grid`
   - `✅ Enchant effect created successfully`
4. 如果有錯誤，查看錯誤訊息
